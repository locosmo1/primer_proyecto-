/* import { get } from "core-js/core/dict";

import { getAuth } from "firebase/auth";

export default function () {
  return {
    comprados: [],
    nombre: "jhonatan",
    auth: getAuth(),
    count: 100,
  }
} */